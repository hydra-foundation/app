# Production overrides
# Use with: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
# A host-level reverse proxy (e.g. Caddy/Traefik) handles TLS on 80/443.

# BUILD_MODE and prod.ini are not repeated here: they are what the base already
# does. What is left is what only production wants — restart policies, memory
# limits, the real-ip config and Redis's eviction policy.

services:
  php:
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2048M

  scheduler:
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M

  nginx:
    restart: unless-stopped
    environment:
      # The reverse proxy in front of this stack: one address or CIDR block.
      # Only this peer's X-Forwarded-For is believed; everything else keeps the
      # address nginx actually accepted. Set NGINX_TRUSTED_PROXY in .env to the
      # proxy's address on the Docker network, NOT to the bridge subnet: a
      # published port makes external callers arrive from the bridge gateway,
      # so a subnet-wide range would let any client name itself.
      NGINX_TRUSTED_PROXY: ${NGINX_TRUSTED_PROXY:?set NGINX_TRUSTED_PROXY to the reverse proxy address}
    volumes:
      # envsubst renders templates/*.template into conf.d/ at container start.
      - ./docker/nginx/real-ip.conf.template:/etc/nginx/templates/real-ip.conf.template:ro
    deploy:
      resources:
        limits:
          memory: 128M

  db:
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 768M

  redis:
    restart: unless-stopped
    # noeviction, not allkeys-lru. These keys are rate-limit counters, and an
    # evictable counter is a budget anyone can clear: fill the instance and
    # every limit in the application resets. Under pressure Redis instead
    # refuses the write, the store raises, and the limiter fails closed.
    command: >
      redis-server
      --maxmemory 128mb
      --maxmemory-policy noeviction
      --save 900 1
      --save 300 10
    deploy:
      resources:
        limits:
          memory: 192M
