# Base stack for the Hydra app skeleton.
#
# During local dev app/vendor/hydra/{core,http,...} are relative symlinks (via
# Composer path repos) that resolve to ../../core, ../../http, ... beside app/.
# The container therefore mounts the WHOLE parent dir (../) at /var/www/html, so
# those symlinks resolve, with the web root pinned to app/public. Run compose
# from this directory so it reads app/.env.
#
#
# The Compose project name is left unset on purpose: it defaults to this
# directory's name (app), so a copy of this stack in any sibling project is
# namespaced correctly with nothing to configure.
#
#   docker compose up -d            # base only
#   ./bin/dev up -d --build         # base + dev (Traefik) — recommended

services:
  php:
    build:
      context: ./docker/php
      args:
        # Default to dev; docker-compose.prod.yml overrides to prod.
        BUILD_MODE: ${BUILD_MODE:-dev}
    volumes:
      - ../:/var/www/html
      # Mount dev.ini in development (default); prod.yml swaps in prod.ini.
      - ./docker/php/dev.ini:/usr/local/etc/php/conf.d/zz-env.ini:ro
    networks:
      - net

  nginx:
    image: nginx:alpine
    sysctls:
      - net.ipv4.tcp_tw_reuse=1
    volumes:
      - ../:/var/www/html
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php
    networks:
      - net

  db:
    image: mariadb:11
    restart: "no"
    environment:
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MARIADB_DATABASE: ${DB_NAME}
      MARIADB_USER: ${DB_USERNAME}
      MARIADB_PASSWORD: ${DB_PASSWORD}
      TZ: ${APP_TIMEZONE:-UTC}
    volumes:
      - ./docker/db_data:/var/lib/mysql
    networks:
      - net

  redis:
    image: redis:7-alpine
    restart: "no"
    volumes:
      - redis_data:/data
    networks:
      - net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  net:

volumes:
  redis_data:
